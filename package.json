{
  "name": "frontend",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "client": "cd frontend && npm start",
    "server": "cd backend && node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
  
  
}
